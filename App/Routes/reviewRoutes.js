// Importing all the Dependencies and Modules.
import express from "express";
const router = express.Router();

// Defining Review Routes.
router.post('/api/user/reviews' , );
router.get('/api/user/reviews/restaurant/:restaurantId' , );
router.get('/api/user/reviews/user/:userId' , );
router.get('/api/user/reviews/:reviewId' , );
router.put('/api/user/reviews/:reviewId' , );
router.delete('/api/user/reviews/:reviewId' , );

// Exporting all the Dependencies and Modules.
export default router;